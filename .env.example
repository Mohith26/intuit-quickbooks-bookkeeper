DATABASE_URL="postgresql://autoledger:autoledger@localhost:15544/autoledger?schema=public"
# Optional: enables the real LLM function-calling fallback stage of the categorizer.
# Without it, unmatched-by-rules transactions route straight to the human review queue.
ANTHROPIC_API_KEY=
